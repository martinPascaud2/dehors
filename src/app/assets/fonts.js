import { Vampiro_One } from "next/font/google";
import { Special_Elite } from "next/font/google";

export const vampiro = Vampiro_One({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
});

export const specialElite = Special_Elite({
  weight: ["400"],
  subsets: ["latin"],
  display: "swap",
});
